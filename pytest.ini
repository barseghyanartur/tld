[pytest]
norecursedirs=
    *.egg
    .git
    .tox
    .env
    _sass
    build
    dist
    migrations
python_files =
    test.py
    test_*.py
    tests.py
python_paths =
    src
    examples/gae
addopts=
   --cov=tld
   --ignore=.tox
   --ignore=requirements
   --ignore=release
   --ignore=bugs
   --ignore=tests
   --cov-report=html
   --cov-report=term
   --cov-report=annotate
   --cov-append
   --capture=no
